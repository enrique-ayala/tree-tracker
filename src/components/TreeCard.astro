---
const { name, species, plantDate, updates } = Astro.props;
---

<div style="border: 1px solid #ccc; padding: 1rem; border-radius: 10px;">
  <h2>{name}</h2>
  <p><strong>Species:</strong> {species}</p>
  <p><strong>Planted:</strong> {plantDate}</p>

  <h3>Progress updates</h3>
  <ul>
    {updates.map(update => (
      <li style="margin-bottom: 1rem;">
        <p><strong>{update.date}</strong> - {update.title}</p>
        {update.image && <img src={update.image} alt={update.title} width="200" style="border-radius: 8px;" />}
        <p><em>Height:</em> {update.height_cm} cm</p>
        <p><em>Weather:</em> {update.weather}</p>
        <p>{update.notes}</p>
      </li>
    ))}
  </ul>
</div>
